{
  "name": "pro-query",
  "version": "0.1.0",
  "description": "Local robot-friendly CLI for querying ChatGPT from a consented logged-in browser session.",
  "type": "module",
  "homepage": "https://github.com/ratacat/pro-query#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ratacat/pro-query.git"
  },
  "keywords": [
    "chatgpt",
    "cli",
    "bun",
    "agents"
  ],
  "bin": {
    "pro": "./src/cli.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check": "bun test && tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "typescript": "^5.9.3"
  }
}
